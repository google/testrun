---
- hosts: all
  become: true
  tasks:
    # Common setup tasks
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install Python3, pip, venv, and setcap utility
      apt:
        name:
          - python3
          - python3-pip
          - python3-venv
          - libcap2-bin
          - net-tools
        state: present

    # Set up networking on ens5
    - name: Install DHCP client
      apt:
        name: isc-dhcp-client
        state: present
        update_cache: yes

    - name: Run dhclient on ens5 (in background)
      shell: nohup dhclient ens5 &
      async: 0
      poll: 0

    # BACnet stack setup
    - name: Install BACnet dependencies
      apt:
        name:
          - git
          - gcc
          - make
          - bison
          - flex
          - libpcap-dev
        state: present
        update_cache: yes

    - name: Clone BACnet stack repository
      git:
        repo: https://github.com/bacnet-stack/bacnet-stack.git
        dest: /opt/bacnet-stack
        version: master

    - name: Build BACnet stack
      make:
        chdir: /opt/bacnet-stack
        target: all

    - name: Copy bacserv-wrapper.sh
      copy:
        dest: /usr/local/bin/bacserv-wrapper.sh
        mode: '0755'
        content: |
          #!/bin/bash
          IFACE=ens5
          while true; do
            IP=$(ip -4 addr show $IFACE | awk '/inet / {print $2}' | cut -d/ -f1)
            if [ -n "$IP" ]; then
              export BACNET_IP=$IP
              exec /opt/bacnet-stack/bin/bacserv
            fi
            sleep 2
          done

    - name: Create systemd unit for bacserv
      copy:
        dest: /etc/systemd/system/bacserv.service
        mode: '0644'
        content: |
          [Unit]
          Description=BACnet Server (auto-restart on IP change)
          After=network-online.target
          Wants=network-online.target

          [Service]
          Type=simple
          ExecStart=/usr/local/bin/bacserv-wrapper.sh
          Restart=always
          RestartSec=5

          [Install]
          WantedBy=multi-user.target

    - name: Reload systemd and enable bacserv service
      systemd:
        name: bacserv
        enabled: yes
        state: restarted
        daemon_reload: yes

    - name: Check if bacserv process is running
      shell: pgrep -fl bacserv
      register: bacserv_process
      changed_when: false

    - name: Show information about bacserv process
      debug:
        var: bacserv_process.stdout_lines

    - name: Show last lines of bacserv log
      shell: tail -n 20 /opt/bacnet-stack/bacserv.log
      register: bacserv_log
      changed_when: false
      ignore_errors: true

    - name: Display bacserv log
      debug:
        var: bacserv_log.stdout_lines

    # Modbus TCP server setup

    - name: Create venv for modbus
      become: no
      command: python3 -m venv /home/vagrant/modbus-venv
      args:
        creates: /home/vagrant/modbus-venv

    - name: Create venv for modbus
      become: no
      command: python3 -m venv /home/vagrant/modbus-venv
      args:
        creates: /home/vagrant/modbus-venv

    - name: Install pyModbusTCP in venv
      become: no
      command: /home/vagrant/modbus-venv/bin/pip install pyModbusTCP

    - name: Find real python binary path
      shell: readlink -f /home/vagrant/modbus-venv/bin/python
      register: real_python_path

    - name: Allow system python to bind to port 502
      become: true
      command: setcap 'cap_net_bind_service=+ep' {{ real_python_path.stdout }}

    - name: Copy Modbus TCP server script (pyModbusTCP)
      copy:
        src: ./python/src/modbus_server.py
        dest: /home/vagrant/modbus_server.py
        owner: vagrant
        group: vagrant
        mode: '0644'

    - name: Ensure modbus server is running (systemd)
      copy:
        dest: /etc/systemd/system/modbus-server.service
        content: |
          [Unit]
          Description=Modbus TCP Server (pyModbusTCP)
          After=network.target

          [Service]
          Type=simple
          User=vagrant
          ExecStart=/home/vagrant/modbus-venv/bin/python /home/vagrant/modbus_server.py
          Restart=always

          [Install]
          WantedBy=multi-user.target

    - name: Reload systemd
      systemd:
        daemon_reload: yes

    - name: Enable and start modbus-server
      systemd:
        name: modbus-server
        enabled: yes
        state: started

    # NTP client setup

    - name: Install chrony
      apt:
        name: chrony
        state: present
        update_cache: yes

    - name: Remove static NTP servers from chrony.conf
      lineinfile:
        path: /etc/chrony/chrony.conf
        regexp: '^(server|pool)'
        state: absent

    - name: Ensure sourcedir for DHCP is present
      lineinfile:
        path: /etc/chrony/chrony.conf
        line: 'sourcedir /run/chrony-dhcp'
        state: present

    - name: Restart chrony
      service:
        name: chrony
        state: restarted
        enabled: yes

    - name: Wait for chrony to sync sources
      wait_for:
        timeout: 10

    - name: Show current chrony sources
      command: chronyc sources
      register: chrony_sources

    - name: Print chrony sources
      ansible.builtin.debug:
        var: chrony_sources.stdout

    # DNS request setup

    - name: Create systemd service for curl
      ansible.builtin.copy:
        dest: /etc/systemd/system/curl-google.service
        content: |
          [Unit]
          Description=Curl google.com

          [Service]
          Type=oneshot
          ExecStart=/usr/bin/curl -s -L -o /dev/null -w "%{http_code}\n" https://www.google.com

    - name: Create systemd timer for curl
      ansible.builtin.copy:
        dest: /etc/systemd/system/curl-google.timer
        content: |
          [Unit]
          Description=Run curl google.com every 5 seconds

          [Timer]
          OnBootSec=5
          OnUnitActiveSec=5

          [Install]
          WantedBy=timers.target

    - name: Reload systemd daemon
      ansible.builtin.systemd:
        daemon_reload: yes

    - name: Enable and start curl-google.timer
      ansible.builtin.systemd:
        name: curl-google.timer
        enabled: yes
        state: started

    # mDNS traffic setup

    - name: Ensure avahi-utils is installed
      ansible.builtin.apt:
        name: avahi-utils
        state: present
        update_cache: yes

    - name: Create systemd service for mDNS traffic
      ansible.builtin.copy:
        dest: /etc/systemd/system/mdns-traffic.service
        content: |
          [Unit]
          Description=Generate mDNS traffic using avahi-publish

          [Service]
          Type=oneshot
          ExecStart=/usr/bin/avahi-publish -s "TestService" _http._tcp 8080

    - name: Create systemd timer for mDNS traffic
      ansible.builtin.copy:
        dest: /etc/systemd/system/mdns-traffic.timer
        content: |
          [Unit]
          Description=Run mDNS traffic generator every 5 seconds

          [Timer]
          OnBootSec=5
          OnUnitActiveSec=5

          [Install]
          WantedBy=timers.target

    - name: Reload systemd daemon
      ansible.builtin.systemd:
        daemon_reload: yes

    - name: Enable and start mdns-traffic.timer
      ansible.builtin.systemd:
        name: mdns-traffic.timer
        enabled: yes
        state: started

    # Outbound TLS traffic setup

    - name: Ensure openssl is installed
      ansible.builtin.apt:
        name: openssl
        state: present
        update_cache: yes

    - name: Copy TLS emulation script and make it executable
      ansible.builtin.copy:
        dest: /usr/local/bin/tls_emulate.sh
        mode: '0755'
        content: |
          #!/bin/bash
          # Ждём, пока ens5 не получит IP-адрес
          while true; do
              IP=$(ip -4 addr show ens5 | awk '/inet / {print $2}' | cut -d/ -f1)
              if [[ -n "$IP" ]]; then
                  break
              fi
              sleep 1
          done
          # TLS 1.0
          openssl s_client -connect tls-v1-0.badssl.com:1010 -tls1 -bind "$IP"
          # TLS 1.2
          openssl s_client -connect tls-v1-2.badssl.com:1012 -tls1_2 -bind "$IP"
          # TLS 1.3
          openssl s_client -connect tls13.badssl.com:443 -tls1_3 -bind "$IP"

    - name: Create systemd service for TLS emulation
      ansible.builtin.copy:
        dest: /etc/systemd/system/tls-emulate.service
        content: |
          [Unit]
          Description=Emulate outgoing TLS connections via ens5

          [Service]
          Type=oneshot
          ExecStart=/usr/local/bin/tls_emulate.sh

    - name: Create systemd timer for TLS emulation
      ansible.builtin.copy:
        dest: /etc/systemd/system/tls-emulate.timer
        content: |
          [Unit]
          Description=Run TLS emulation every 5 seconds

          [Timer]
          OnBootSec=5
          OnUnitActiveSec=5

          [Install]
          WantedBy=timers.target

    - name: Reload systemd daemon
      ansible.builtin.systemd:
        daemon_reload: yes

    - name: Enable and start tls-emulate.timer
      ansible.builtin.systemd:
        name: tls-emulate.timer
        enabled: yes
        state: started

    # Install and start nginx for HTTP traffic

    - name: Install nginx
      apt:
        name: nginx
        state: present

    - name: Ensure nginx is running
      service:
        name: nginx
        state: started
        enabled: yes

    # Install firewall

    - name: Install UFW firewall
      apt:
        name: ufw
        state: present
        update_cache: yes

    - name: Reset UFW to default
      command: ufw --force reset

    - name: Set default UFW policy to deny incoming
      command: ufw default deny incoming

    - name: Set default UFW policy to allow outgoing
      command: ufw default allow outgoing

    - name: Allow all incoming traffic on ens4
      command: ufw allow in on ens4

    - name: Allow all outgoing traffic on ens4
      command: ufw allow out on ens4

    - name: Allow 443/tcp incoming on ens5
      command: ufw allow in on ens5 to any port 443 proto tcp

    - name: Allow all outgoing traffic on ens5
      command: ufw allow out on ens5

    - name: Allow BACnet UDP port 47808 incoming on ens5
      command: ufw allow in on ens5 to any port 47808 proto udp

    - name: Allow BACnet UDP port 47808 outgoing on ens5
      command: ufw allow out on ens5 to any port 47808 proto udp

    - name: Allow BACnet UDP broadcast on ens5
      command: ufw allow in on ens5 to 255.255.255.255 port 47808 proto udp
      ignore_errors: yes

    - name: Allow Modbus TCP port 502 incoming on ens5
      command: ufw allow in on ens5 to any port 502 proto tcp

    - name: Allow Modbus TCP port 502 outgoing on ens5
      command: ufw allow out on ens5 to any port 502 proto tcp

    - name: Enable UFW
      command: ufw --force enable

    - name: Show UFW status
      command: ufw status verbose
      register: ufw_status

    - name: Print UFW status
      debug:
        var: ufw_status.stdout